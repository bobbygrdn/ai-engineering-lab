{
    "name": "update_password",
    "description": "Update the password for the account.",
    "parameters": {
        "password": { "type": "string" }
    },
    "returns": {
        "success": { "type": "boolean" },
        "message": { "type": "string" }
    }
}